function Projects({lightMode}) {

    return(
        <section style={{height: "100vh", width: "100vw"}} className={"component"} id={'projects'}>
            My Projects
        </section>
    )
}
export default Projects