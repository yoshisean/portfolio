function About({lightMode}) {
    return(
        <section style={{height: "100vh", width: "100vw"}} className={"component"} id={'about'}>
            About Page
        </section>
    )
}
export default About