function Contact({lightMode}) {

    return(
        <section style={{height: "100vh", width: "100vw"}} className={"component"} id={'contact'}>
            Contact Details
        </section>
    )
}
export default Contact